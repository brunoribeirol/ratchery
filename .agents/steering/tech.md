# Tech

Stack and tooling an agent should assume without re-deriving it every session.

## Project-specific stack

- Python 3.11+ standard-library CLI under `lib/`.
- POSIX shell installer and integration tests.
- GitHub Actions CI/release automation.

Current generated language/capability facts: `.agents/state/project-profile.md`.

## Commands

See `docs/COMMANDS.md` (kept in sync automatically).
