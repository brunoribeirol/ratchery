# Profiles and Context

The runtime profiles source files, lines, package roots, monorepo signals, and domain capabilities. Size is a routing signal, not a quality score. Initial file budgets are starting bounds, not quotas.

Conditional Skills are installed only when signals justify them. The setup does not auto-add MCP schemas merely because a repository is medium/large.
