# Projects Workspace

New projects live under `~/Projects` in `personal`, `academic`, `learning`, `work`, `experiments`, or `archived`. Legacy repositories may remain directly under Documents.

Use category by current purpose, not technology. Each repository has its own Git root. Do not make `Projects/` itself a monorepo.
