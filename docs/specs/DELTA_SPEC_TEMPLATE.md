# Delta Spec: {{title}}

Mode: **openspec-light** (T1/T2 default). Describe only what changes, not the whole system.
Reimplements the pattern researched from OpenSpec's diff-based context strategy
(see `docs/research-external.md` section 4) -- this is a native template, not a
dependency on that project.

## Why

<one paragraph: the problem or requirement driving this change>

## What changes

<the delta only -- new/changed/removed behavior, contracts, schemas, endpoints.
Do not restate what is unaffected.>

## Affected contracts

<APIs, schemas, CLI flags, config keys, or file formats this touches>

## Out of scope

<explicitly what this change does NOT do, to bound review>

## Tasks

- [ ] <task>
- [ ] <task>

## Validation

<the exact commands/tests that prove this delta works>

## Status

- [ ] Proposed
- [ ] In progress
- [ ] Implemented
- [ ] Archived (link the merged PR/commit)
