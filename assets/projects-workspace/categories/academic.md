# Academic Projects

Use this directory for university repositories, TCC implementations, academic research, scientific experiments, and institution-related deliverables.

Use `learning/` instead when the repository exists mainly to follow a course or tutorial.

Create a project:

```bash
ratchery new project-name --category academic
```
