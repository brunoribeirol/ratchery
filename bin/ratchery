#!/usr/bin/env bash
set -euo pipefail
SELF="$(python3 -c 'import os,sys; print(os.path.realpath(sys.argv[1]))' "$0")"
ROOT="$(cd "$(dirname "$SELF")/.." && pwd)"
exec python3 "$ROOT/lib/agent_workspace.py" "$@"
